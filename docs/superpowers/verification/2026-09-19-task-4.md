# Task 4 Verification - Checkout 2I-6

## Scope

Final verification record for Task 2-3 checkout flow after PR #5 was merged.

## Static checks

- `paket/index.php`: existing package selection contract.
- `daftar.php`: selected package is validated server-side against ACTIVE packages, preserved through POST, stored in session, then redirected to checkout.
- `checkout.php`: authenticated checkout, ACTIVE package re-query, database price snapshot, BANK_TRANSFER + PENDING payment creation, 24-hour expiry, CSRF, and duplicate pending protection.
- `tests/test_registration_package.php`: registration package contract.
- `tests/test_checkout.php`: checkout/payment contract.

## Runtime verification status

PHP/MySQL runtime execution was not available through the GitHub connector environment, so no runtime test output is claimed here.

## CI status

The merged Task 2-3 commit has no GitHub Actions workflow runs or status checks reported by the connected GitHub repository.

## Database verification

Production `SHOW COLUMNS FROM payments` and end-to-end browser verification must be run against the deployed MySQL instance.

## Manual verification checklist

1. `/paket/` lists only ACTIVE packages.
2. Package selection opens registration with the selected package retained.
3. Invalid/inactive package IDs are rejected.
4. Successful registration creates account, store, and ADMIN membership atomically.
5. Successful registration redirects to checkout.
6. Checkout shows the database package price.
7. Submission creates one PENDING BANK_TRANSFER payment.
8. Refresh/re-submit does not create another active pending payment for the same selection.
9. Changing the package price after payment creation does not alter the stored `payments.amount`.
